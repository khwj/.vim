# Just My VIM Configurations
My personal vim configurations including syntax, colors and plugins

## Install
```sh
git clone --depth 1 https://github.com/khwj/.vim.git ~/.vim && ln -s ~/.vim/.vimrc ~/.vimrc
```
